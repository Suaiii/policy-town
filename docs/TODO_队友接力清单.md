# TODO：合肥产业投资推演系统队友接力清单

## 本轮进度更新（2026-08-13）

本轮已完成 Agent Memory 的第一版持久化闭环：

- [x] 新增独立 `MemoryStore`，与历史事实 SQLite 分离；历史数据库继续只读。
- [x] `RealityGraph` 按 `run_id` 写入 `reality_graph_records`，支持 `stage_id`、`visibility`、`available_at` 查询过滤。
- [x] 企业 `EnterpriseMemoryState` 按 `run_id/company_id/stage_id` 写入快照表，并保留意图事件历史。
- [x] 新局初始化、阶段结束快照和进程重启恢复已接入 `InvestmentEngine`。
- [x] Replay 阶段不写回运行时 Graph、Memory 或意图事件。
- [x] 按数据接入交接说明接通赛维母公司信用与光伏价格周期：`parent_financing_health` 与 `supply_pressure` 均由截止日前 observation 派生，且不混合美元/亿元口径。
- [x] 新增重启恢复、图谱可见性/截止日过滤和 Replay 隔离回归测试；当前全套测试 `81` 项通过。

当前准确口径：企业已有跨阶段私有 Memory、信念更新、意图历史和可持久化 RealityGraph 运行骨架；完整 Persona 的冲突处理、遗忘/置信度衰减、多方关系认知和更细的履约后策略校准仍未完成。

> 更新时间：2026-08-13（前端接入启动，加入沙盘反映决策变化等前端任务）
> 当前分支：`main`  
> 文档性质：执行清单（Reference + How-to）  
> 当前原则：政府 Agent、协商闭环和规则结算后端 MVP 已通；企业私有状态注入、产品前端和上线性能优化仍是后续工作。前端已启动，任务见第 7 节，设计见 `docs/前端可视化指导.md`。

## 0. 接手前先做

- [ ] 阅读 `README.md` 的“当前唯一有效的产品阅读顺序”。
- [ ] 阅读 `docs/产品文档.md`，重点看“截止日冻结推演”和“一图两账”。
- [ ] 阅读 `docs/评委沟通_历史信息防泄露产品亮点.md`。
- [ ] 阅读 `docs/合肥财政数据来源与阶段预算派生审计报告.md`。
- [ ] 查看 `git status --short`；工作区可能存在其他队友的未提交改动，不要覆盖、回滚或一起提交。
- [ ] 运行基线测试：

```powershell
$py='C:\Users\ZHUyi\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
& $py scripts\seed_hefei_database.py
& $py scripts\audit_hefei_database.py
& $py -m unittest discover -s tests -q
```

基线要求：数据库 `integrity=ok`，全部测试通过。

## 1. 当前已完成

- [x] 合肥产业投资独立契约与后端状态机。
- [x] SQLite Schema、Seed、审计脚本和只读数据仓库。
- [x] 真实财政、产业、企业和项目观察进入 Context。
- [x] `effective_date` 与 `information_available_date` 分离。
- [x] 截止日过滤、withheld 历史结果隔离和泄漏审计。
- [x] 来源 URL、本地归档、抓取时间和 SHA-256 追溯链。
- [x] 2008—2015 政府性基金收支连续序列；2015 债务数据交叉验证。
- [x] 2008—2015 财政本年收支净额；补入 2012、2014、2015 历史债务余额。
- [x] 补入已核验的项目级资金证据：京东方 6 代线政府平台实缴股本、8.5 代线国资登记股本、赛维高新区平台委托贷款本金。
- [x] 补入合肥建投 2011 年 40 亿元现金增资和 2015 年 0.44 亿元国资预算增资证据。
- [x] 赛维 LDK（CASE-04）失败案例深校准：母公司 2008/2009 财务由 Wikipedia 二手升级为 SEC EDGAR 20-F 一手（A 级）；补入多晶硅价格周期（2007–2009）与硅片售价/销量；补 2011 价格崩盘、2014 美国 Chapter 11、2015 中国破产三个失败里程碑与 2011 光伏过剩事件；导出 `data/historical_cases/hefei_ldk_2010/` 回放包（截止 `2010-08-30`，pre_cutoff=72 / withheld=143，targets=failure+invest）。
- [x] 后公开评级报告不得反向进入早期 Agent Context。
- [x] 财政点数明确标记为 `scenario_assumption`，不冒充真实可投资亿元。
- [x] 完成第一条真实决策基线：京东方合肥 6 代线 S1 的动作、日期、金额/持股、条件、里程碑和来源已结构化，并接入 Replay 对账。
- [x] 纠正 S1 信息可用日：2008-09-12 签署、2008-09-13 公开的框架协议不得进入决策日盲推演 Context。
- [x] Replay 对现实亿元/持股与游戏财政点数执行单位隔离；无同口径输出时标为 `not_evaluable`。
- [x] 产品主线文档、财政审计报告和评委沟通话术。
- [x] 本地 `main` 已合并当前开发成果。
- [x] 最近一次基线：43 项测试通过。
- [x] 四部门 Agent 契约、定向质询、联席双方案和确定性 fallback。
- [x] 企业核验、策略性回应、政府条件单、一次反提案和规则结算。
- [x] `run_id` 隔离的判断账与承诺账，以及未来证据隔离。
- [x] 每阶段单项承诺随访、违约暂停追加保护和终局 `story_timeline`。
- [x] 统一截止日：S1 `2008-09-12`、S2 `2011-06-30`、S3 `2014-06-30`、S4 `2016-12-31`。
- [x] 当前 Agent 回合测试：48 项全部通过。

## 1.1 Agent 框架计划对照（2026-08-13）

| 计划模块 | 当前状态 | 准确说法 |
|---|---|---|
| 政府四部门契约 | 已完成 MVP | 财政、经信、科技、发改均有冻结输入、输出 Schema、证据约束和确定性 fallback。 |
| 政府 Agent 运行骨架 | 已完成 MVP | 支持独立初审、有限质询、联席方案和可选的 OpenAI 兼容模型适配器。 |
| 企业 Agent 契约 | 已完成基础 MVP | 六家企业已有版本化 `EnterprisePrivateState`、阶段 Context 和结构化意图；企业模型调用已接入，企业多阶段意图历史和新鲜状态已开始写入私有 Memory。 |
| 一图两账 Memory | 已完成持久化 MVP | 独立 `MemoryStore` 已将 `RealityGraphRecord`、企业 Memory 快照和意图事件写入单独 SQLite；支持 `run_id` 隔离、阶段/可见性/截止日查询、重启恢复与 Replay 不写回。更完整的事实关系图和 Persona 校准仍待实现。 |
| 政企协商 | 已完成后端 MVP | 已通过“核验问题→企业回应→政府条件→一次反提案→确认→规则结算”。 |
| 三个核心案例 | Persona 基础接入 | 京东方、赛维 LDK、长鑫已从版本化企业设置注入私有状态、利益目标、风险偏好和披露边界；跨阶段认知循环已有可运行骨架，仍需真实企业行为校准。 |
| 投后随访与终局时间线 | 已完成后端 MVP | 承诺履约、违约暂停追加和 `story_timeline` 已运行，前端尚未呈现。 |
| 性能与上线化 | 基础实现 | OpenCode Go 重试、超时、结果内存缓存、模型/Prompt/Context/seed 记录已接入；4GB/3Mbps 压测与服务器部署配置仍待实施。 |

## 2. P0：必须优先完成

### P0-1 冻结政府四部门 Agent 契约

建议负责人：Agent / 后端。

- [x] 将当前四类专业研判正式命名为财政、经信、科技、发改部门。
- [x] 定义部门输入：冻结 Context、部门 KPI、红线、可用工具和缺失信息。
- [x] 定义部门输出：初审立场、主张、证据、置信度、红线和可接受条件。
- [x] 保留现有确定性研判作为模型失败时的 fallback。
- [x] 禁止 Agent 直接生成或修改财政点数、企业财务和建设进度。

验收：相同输入和 seed 下结构化输出可复核；所有重要主张绑定 evidence ID 或缺失声明。

### P0-2 冻结企业 Agent 契约与“一图两账”

建议负责人：Agent / 后端。

- [x] 将企业固定决策底色落成结构化私有状态：目标、风险偏好、扩张惯性（六家企业已接入版本化 JSON 配置）。
- [x] 实现现实图谱的冻结输入：只读事实、事件、来源和可见时间（当前由 SQLite Context 承载，独立 `RealityGraph` 实体待建）。
- [x] 实现判断账：市场、融资、技术执行和政府履约判断。
- [x] 实现承诺账：政企承诺、到期阶段、触发条件和履约状态。
- [x] 每个 `run_id` 隔离模拟记忆，不能污染现实证据或其他运行（已有独立状态与回归测试）。
- [x] 为京东方、赛维 LDK、长鑫分别登记一个关键未穿透命题（当前为后端命题配置，企业设定自动注入待完成）。

验收：任一企业动作能够追溯至事实、判断和承诺；模拟生成的信息不会写回现实来源表。

### P0.5 企业私有状态正式接入（下一个 Agent 主体任务）

- [ ] 将 `enterprise-agent-settings.md` 转为版本化结构化配置，不在运行时解析 Markdown。
- [x] 增加 `EnterprisePrivateState`：身份、利益目标、财务/技术/客户私有状态、风险偏好和扩张惯性。
- [x] 为六家企业接入阶段化 Context，并按 S1—S4 截止日过滤可见信息。
- [x] 将企业回应改为受私有状态和利益目标驱动，支持披露、区间、拒答、交换条件四种回应（确定性 Persona runtime 已接入；模型企业回应仍需继续校准输出协议）。
- [ ] 将 `scenario_assumption` 与公开事实、演绎原因和游戏参数分离，不允许企业编造已确实订单、资金或技术成果。
- [x] 增加政府 Context 与企业私有 Context 隔离测试，确保政府不能直接读取企业私有字段。

### P0-3 实现可审计的政企协商最小闭环

建议负责人：Agent / 产品全栈。

- [x] 根据关键未穿透命题生成核验问题卡。
- [x] 企业只能以“披露、区间、拒答、交换条件”回应。
- [x] 将玩家选择转换成结构化政府条件单。
- [x] 允许企业进行一次结构化反提案。
- [x] 最终条件写入承诺账，并交给规则引擎结算。
- [x] 模型超时或 Schema 校验失败时使用确定性默认路径。

验收：完成“核验问题 → 企业回应 → 政府条件 → 企业反提案 → 最终承诺 → 规则结算”全过程，日志可复核。

### P0-4 固化“截止日冻结推演”演示用例

建议负责人：产品 / 测试 / 后端。

- [x] 固定 S1 京东方演示截止日 `2008-09-12`。
- [x] 准备一条当时可知证据和两条真实但后来才公开的证据。
- [x] 输出机器可读的过滤原因：材料公开日、阶段截止日、拒绝原因。
- [x] 玩家模式只显示“当时不可知”；审计模式显示完整过滤信息。
- [x] 终局 Replay 再解锁年报、项目节点和真实结果。
- [x] 增加测试：删除关键证据后，至少一个部门立场或条件合理变化。

验收：现场能够证明系统拒绝一条“真实但当时不可知”的材料，并在终局正确解锁。

### P0-5 第一条真实决策基线

- [x] 固化京东方合肥 6 代线 S1 `decision_baseline.yaml`。
- [x] 建立五要素、日期、单位和来源追溯门禁。
- [x] 在 `targets.yaml` 登记 `action_baseline` 引用。
- [x] Replay 输出动作、阶段时点和不可评价字段的结构化对账，不新增虚假综合分。
- [x] 增加缺字段、缺来源、次日公开隔离和点数/亿元禁换算测试。
- [ ] 第二核心问题：定义盲推演逻辑。必须隐藏真实政府动作，仅提供截止日前材料，再与同一基线比较；暂不预设算法。

验收：给 S1 的 `company_a` 输入 `invest` 时动作和时点匹配；不给动作时明确不匹配；现实金额、持股和条件在没有同口径引擎输出前均为 `not_evaluable`。

## 3. P1：数据与财政增强

建议负责人：数据工程。

- [ ] 查找并归档 2007 年合肥政府性基金收入与支出。（真实基线阶段暂缓继续补数；本轮仍缺原始决算表，未用估算值填补）
- [ ] 查找 2008—2016 土地出让收入的逐年拆分。
- [ ] 查找国有资本经营预算逐年收支数值、年末财政结余和其余年份政府债务。
- [ ] 查找京东方、赛维、长鑫项目完整的政府实际出资、分期与后续承诺；本轮已先行落库可核验的部分股本和委托贷款证据。
- [ ] 优先使用财政决算、人大预算报告、审计报告和上市公司公告。
- [ ] 评级报告仅作二次披露，保持 `provisional`；取得官方原表后新增来源版本，不静默覆盖。
- [ ] 每次新增来源都更新 `manifest.json`、SHA-256、Seed 和财政审计报告。
- [ ] 不同层级、不同地域范围和不同预算口径不得拼接成同一序列。
- [ ] （暂缓 · 迁移服务器前处理）数据库路径去绝对化：`policytown/investment/real_data.py` 用 `Path.resolve()` 把 SQLite 路径写成机器绝对路径，并进入 `demo_run.json` 等产物的 `database_path` 字段；上线前改为相对路径或由环境变量/配置项注入，避免本地绝对路径泄漏进仓库产物。

验收：每个新增数值都能回到 `source_id → URL → archived_path → SHA-256`；无法核验的字段保持缺失。

## 4. P1：部门通信与联席会议

建议负责人：Agent / 后端。

- [x] 实现部门之间的定向 `Challenge / Response`。
- [x] 仅在真实分歧存在时触发质询，避免无限聊天。
- [x] 实现结构化的立场变化记录，保存变化前后、证据和新增条件。
- [x] 输出多方案、共识、分歧与少数意见。
- [x] 玩家可以采纳、修改或拒绝联席方案（当前通过 `NegotiationChoice` 选择或拒绝）。
- [x] 只有玩家确认的结构化条件进入规则引擎。

验收：删除关键质询或证据后，最终条件出现可解释变化；系统不强制虚假共识。

> 状态说明：该模块的后端 MVP 已完成；后续只需在企业私有状态接入后重新校验质询是否真正改变企业条件。

## 5. P1：投后随访与单线终局

建议负责人：Agent / 产品全栈。

- [x] 每阶段只随访承诺账中最重要的一项到期承诺。
- [x] 记录已完成、违约或证据不足。
- [x] 将履约结果写回本局承诺状态。
- [x] 违约时暂停追加；重组或退出保留为玩家决策。
- [x] 终局生成“核验—谈判—承诺—里程碑—结果”时间线。
- [x] Replay 展示关键命题复盘，不仅仅显示一个总分。

验收：S1 的承诺能够在后续阶段改变财政、企业动作和政府条件。

## 6. P2：测试与交付质量

建议负责人：后端 / 测试。

- [x] 给 S1—S4 分别增加信息截止测试。
- [x] 增加后公开报告反向泄漏测试。
- [x] 增加跨 `run_id` Memory 污染测试。
- [x] 增加预算守恒、承诺履约和状态变化对账测试。
- [ ] 增加来源归档有效性测试：PDF 头、HTML 长度、SHA-256。
- [x] 增加缺失值不自动猜测测试。
- [x] 增加模型超时、无效 JSON 和断网降级测试。
- [x] 固定一份演示运行输出，记录 `context_hash`、模型版本、Prompt 版本和 seed（当前输出已保存 seed/context_hash，模型运行版本注记待完善）。

验收：全套测试通过；相同输入可重复；任何未来证据都会使审计失败。

## 6.1 下一个实现批次：企业 Agent 框架

- [x] 先完成三个核心案例：京东方、赛维 LDK、长鑫；其他三家企业已接入同一通用接口。
- [x] 为每家企业建立不可发送给政府的私有 Context，政府只能通过正式核验问题互动（已由 `EnterpriseMemoryState` 与 Brief 隔离测试覆盖）。
- [x] 将政府四部门摘要、企业回应和反提案挂接到可审计回合轨迹（意图历史、图谱记录和时间线同属一个 run_id）。
- [x] 把 `RealityGraphRecord` 与 `EnterpriseMemoryState` 持久化到独立 SQLite，并保证回放不能混入未来结果。
- [ ] 将企业信念从当前规则参数继续调校为基于历史状态、承诺履约和新证据的完整多阶段决策；本轮已接入母公司信用与价格周期证据。
- [ ] 在 4GB 内存 / 3Mbps 带宽的运行环境中实现结果缓存、请求超时、断网 fallback 和模型/提示词版本记录。
- [ ] 将 `story_timeline`、事实卡、判断卡、承诺卡接入前端，但不让前端计算业务数值。

## 7. 前端接入与可视化（当前重点，已启动）

建议负责人：前端（产品/视觉）。设计原则与视觉逻辑详见 `docs/前端可视化指导.md`。

### 7.1 P0 骨架与真实数据接入

- [ ] 保留 3D 决策桌 + 桌面合肥地图 + 三镜头（沙盘 / 企业核验 / 360°）空间骨架。
- [ ] 接入决策流程主干：决策时点 → 匿名申请 → 四部门研判 → 政企协商 → 企业自主行动 → 统一结算 → 状态变化与承诺账 → 终局 Replay。
- [ ] 左侧冻结 Context、右侧随阶段变化的决策面板、底部资源栏、顶部阶段/步骤/截止日。
- [ ] 企业核验镜头 + 一次反提案（有限状态机，不做无限对话）。
- [ ] 前端只消费后端结构化结果，不计算财政点数、Agent 立场、企业回应、状态变化或评分。

### 7.2 P1 真实结构化输出接入

- [ ] 四部门独立初审（立场 / 主张 / 证据 / 红线 / 置信度 + fallback 标签）。
- [ ] 联席摘要：共识、最大分歧、关键未穿透项、多方案、少数意见。
- [ ] 核验问题卡 + 企业四种回应（披露 / 区间 / 拒答 / 交换条件）明确标出。
- [ ] 政府条件单（显示本轮余额与占比、分期、风险条件分组、结构化选择）+ 企业反提案（原条件 vs 反提案差异清晰）。

### 7.3 P2 产品辨识度可视化

- [ ] 玩家 / 审计两种截止日视图（玩家模式只显“当时不可知”，审计模式说明证据为何被拒）。
- [ ] 证据抽屉 + 过滤原因 + 来源追溯。
- [ ] 承诺账（政府 / 企业答应了什么、何时到期）。
- [ ] 状态变化因果链（政府条件 → 企业行动 → 规则结算 → 指标变化）。
- [ ] 终局双时间线 + 证据解封 + 两组评价（决策过程 + 历史校准，不合成总分）。

### 7.4 沙盘反映决策前后变化（核心，本轮重点）

- [ ] 企业项目地块/企业楼随 `CompanyState` 变化：`construction_progress` → 建设阶段（地基→完工），`production_ramp` → 窗户亮灯比例，`status` → 在建/灯火通明/部分熄灯/全熄/空置，`financial_health` → 楼体色调，`supply_pressure` → 警戒标识。
- [ ] 城市随 `CityMetrics` 变化：`supply_chain_strength` → 车流密度，`talent_supply` → 行人数量，`portfolio_public_value` → 城市亮度。
- [ ] 决策前后变化动画：按 `StateDelta`（before → after）播放，投钱地块长高亮灯、退出地块熄灯变暗，可点开看 `reason_code` + 证据。

### 7.5 P3 视觉增强（最后做）

- [ ] 更复杂 360° 场景、更多企业代表动作、长过场、装饰粒子/景深/辉光、非决策性地图装饰。

### 7.6 前端验收

- [ ] 用户不读说明也能看懂当前阶段与唯一主操作。
- [ ] 三家企业在席位 / 地图 / 面板中的选中状态一致。
- [ ] 四部门分歧一屏可理解，完整证据按需展开。
- [ ] 只能选一个核验问题，企业只反提案一次。
- [ ] 玩家模式不泄露未来材料；审计模式能说明证据为何被拒。
- [ ] 数值变化可追溯至政府动作、企业意图、规则和证据。
- [ ] 评价不用单一总分；前端不自算数值。

## 8. 文件所有权与提交规则

| 范围 | 主要文件 | 注意事项 |
|---|---|---|
| 产品主线 | `docs/产品文档.md` | 发生冲突时以当前主线定义为准 |
| 前端指导 | `docs/前端可视化指导.md` | 只讲原则与硬约束，不限定具体 UI 形式 |
| 前端实现 | `frontend/` | 前端只消费结构化结果，不计算业务数值 |
| 评委表达 | `docs/评委沟通_历史信息防泄露产品亮点.md` | 必须与真实功能一致 |
| 数据工程 | `database/`、`scripts/seed_hefei_database.py` | 修改后必须重建并审计数据库 |
| 来源归档 | `scripts/archive_hefei_sources.py`、`data/source_archive/` | 归档目录为生成物，本地保留；台账需可复建 |
| 投资后端 | `policytown/investment/` | 不让 LLM 直接结算数值 |
| 契约 | `contracts/investment_simulation_v0_1.py` | 改字段前检查 CLI、测试和消费者 |
| 测试 | `tests/test_investment_*.py` | 每个新机制同时增加回归测试 |

提交要求：只提交自己负责的文件；不要顺手提交其他队友正在修改的文件；不要使用 `git reset --hard` 或覆盖未提交改动。

## 9. 推荐领取顺序（当前分工）

| 成员 | 角色 | 下一批任务 |
|---|---|---|
| Suaiii | 开发主体 T（后端 + 集成） | P0-4 截止日演示固化、P0-2 后端 RealityGraph、P2 测试与交付、最终集成 |
| Flyme886 | Agent 架构 A（Agent 辅助） | P0.5 企业私有状态、P0-2 关键未穿透命题、P1 部门通信与联席会议 |
| wgyxnn777 | 前端设计 | 第 7 节前端接入（7.1 骨架 → 7.5 增强），以 `docs/前端可视化指导.md` 为准 |
| 数据（AI 助手辅助 Suaiii） | 数据主体 D | P1 财政增强、来源归档和审计报告更新 |

并行顺序：Flyme886 做 P0.5 企业私有状态，Suaiii 并行做 P0-2 后端 RealityGraph；P0-2 前后端合龙后，Flyme886 转 P1 部门通信、Suaiii 转 P0-4 + P2 测试与集成。

## 10. 完成定义

本轮可以宣布“Agent 推演闭环完成”，必须同时满足：

- [x] 四部门输入输出契约冻结并有 fallback。
- [x] 企业现实图谱、判断账和承诺账可运行。
- [x] 至少一个案例完成政企核验、条件协商和投后随访。
- [x] 截止日冻结演示能够现场拒绝未来信息。
- [x] 任一重要结论可以回到原始报告和 SHA-256。
- [x] 规则引擎独占数值结算，预算和状态变化对账。
- [x] 全套自动化测试通过。
- [x] 评委讲解与产品实际能力一致，不宣称精确预测真实政府结果。
