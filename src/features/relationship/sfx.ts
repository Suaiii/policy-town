export function playCardOpenSound(_mode: 'open' | 'switch'): void {
  // The merged relationship view references an optional sound hook, but the
  // teammate branch does not include an audio asset. Keep the interaction
  // silent instead of blocking the application build.
}
